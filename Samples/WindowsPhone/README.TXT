You need to start the WCF service before running the WindowsPhone project.
To do this open the "WcfService" and select the file "Northwind.svc" and choose "View in Browser".
Now you're ready to run the WindowsPhone project.